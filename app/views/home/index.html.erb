<div class="w-full font-bold">
  <h1 class="text-3xl text-center justify-center">Welcome to the <span class="text-green-700">Evergreen</span> Scheduled tweets</h1>

  <% if current_user %>
  <div class="mt-4 text-center text-lg text-gray-700">
    Welcome back, <span class="font-semibold text-green-600"><%= current_user.first_name %> <%= current_user.last_name %></span>!
  </div>
  <% end %>
</div>